// Same image as opengraph-image.tsx, exported under Next.js's separate
// twitter-image file convention so Twitter/X cards get a preview too
// (some platforms only check for one of the two file names).
export { default, size, contentType } from "./opengraph-image";
